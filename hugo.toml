baseURL = 'https://dominikhofer.me/'
languageCode = 'en-us'
title = 'Dominik Hofer'
timeZone = 'Europe/Zurich'
theme = 'dominik'

[permalinks]
posts = '/:slug/'
races = '/:slug/'

[outputFormats.PostsRSS]
mediaType = 'application/rss+xml'
baseName = 'rss'
isPlainText = true
notAlternative = false

[outputs]
home = ['HTML', 'RSS', 'PostsRSS']

[caches.getresource]
maxAge = 0

[params]
version = '3.0'
location = 'Bern, CH'
description = 'Dominik Hofer — striving for simplicity in a complex world.'
